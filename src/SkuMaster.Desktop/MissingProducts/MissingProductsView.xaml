<UserControl x:Class="SkuMaster.Desktop.MissingProducts.MissingProductsView"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:local="clr-namespace:SkuMaster.Desktop.MissingProducts">
 <Grid>
  <Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
  <TabControl x:Name="ModuleTabs" BorderThickness="0" Background="Transparent">
   <TabItem Header="1  Файли">
    <StackPanel Margin="0,12,0,0" TextElement.FontWeight="Normal">
     <TextBlock Text="Знайдемо товари постачальника, яких немає на сайті та у виключеннях." Style="{StaticResource Hint}" Margin="0,0,0,10"/>
     <Border Style="{StaticResource Card}" Padding="6" Margin="0,0,0,6"><Grid IsEnabled="{Binding CanEdit}"><Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
      <StackPanel VerticalAlignment="Center"><TextBlock Text="1 · Залишки на сайті" FontWeight="SemiBold"/><TextBlock Text="CSV · артикули" Style="{StaticResource Hint}"/></StackPanel>
      <TextBox Grid.Column="1" Text="{Binding SitePath, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="Пошук: файл сайту"/>
      <Button Grid.Column="2" Content="Обрати…" Tag="site" Click="Browse_Click" Margin="8,0,0,0"/>
     </Grid></Border>
     <Border Style="{StaticResource Card}" Padding="6" Margin="0,0,0,6"><Grid IsEnabled="{Binding CanEdit}"><Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
      <StackPanel VerticalAlignment="Center"><TextBlock Text="2 · Постачальник" FontWeight="SemiBold"/><TextBlock Text="Excel · артикул і назва" Style="{StaticResource Hint}"/></StackPanel>
      <TextBox Grid.Column="1" Text="{Binding SupplierPath, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.Name="Пошук: файл постачальника"/>
      <Button Grid.Column="2" Content="Обрати…" Tag="supplier" Click="Browse_Click" Margin="8,0,0,0"/>
     </Grid></Border>
     <Border Style="{StaticResource Card}" Padding="6" Margin="0,0,0,6"><Grid IsEnabled="{Binding CanEdit}"><Grid.ColumnDefinitions><ColumnDefinition Width="180"/><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
      <StackPanel VerticalAlignment="Center"><TextBlock Text="3 · Виключення" FontWeight="SemiBold"/><TextBlock Text="{Binding ExclusionCount, StringFormat=Збережено артикулів: {0:N0}}" Style="{StaticResource Hint}"/></StackPanel>
      <TextBox Grid.Column="1" Text="{Binding ExclusionPath, Mode=OneWay}" IsReadOnly="True" AutomationProperties.Name="Пошук: файл виключень" ToolTip="Файл необов’язковий. Імпорт замінить збережений список. Без імпорту використовуються збережені виключення."/>
      <Button Grid.Column="2" Content="Імпортувати…" Tag="exclusions" Click="Browse_Click" Margin="8,0,0,0"/>
     </Grid></Border>
     <TextBlock Text="Виключення завантажуються з пам’яті програми. Новий імпорт замінює список; додавання з результатів доповнює його." Style="{StaticResource Hint}"/>
    </StackPanel>
   </TabItem>
   <TabItem Header="2  Налаштування">
    <ScrollViewer VerticalScrollBarVisibility="Auto"><StackPanel x:Name="InputSettings" Margin="0,12,0,0" IsEnabled="{Binding CanEdit}" TextElement.FontWeight="Normal">
     <TextBlock Text="Номери колонок і рядків починаються з 1. Якщо є заголовок — перший рядок товарів зазвичай 2." Style="{StaticResource Hint}"/>
     <UniformGrid Columns="3">
      <StackPanel Margin="0,0,16,0"><TextBlock Text="Сайт · CSV" FontWeight="SemiBold" Style="{StaticResource Label}"/>
       <TextBlock Text="Колонка артикула" Style="{StaticResource Label}"/><TextBox Text="{Binding Settings.Site.SkuColumn, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True, NotifyOnValidationError=True}"/>
       <TextBlock Text="Перший рядок товарів" Style="{StaticResource Label}"/><TextBox Text="{Binding Settings.Site.FirstDataRow, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True, NotifyOnValidationError=True}"/>
       <TextBlock Text="Роздільник" Style="{StaticResource Label}"/><ComboBox ItemsSource="{x:Static local:MissingProductsViewModel.Delimiters}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding Settings.Site.Delimiter}" SelectionChanged="Settings_Changed"/>
       <TextBlock Text="Кодування" Style="{StaticResource Label}"/><ComboBox ItemsSource="{x:Static local:MissingProductsViewModel.Encodings}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding Settings.Site.EncodingName}" SelectionChanged="Settings_Changed"/>
      </StackPanel>
      <StackPanel Margin="0,0,16,0"><TextBlock Text="Постачальник · Excel" FontWeight="SemiBold" Style="{StaticResource Label}"/>
       <TextBlock Text="Колонка артикула" Style="{StaticResource Label}"/><TextBox Text="{Binding Settings.Supplier.SkuColumn, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True, NotifyOnValidationError=True}"/>
       <TextBlock Text="Колонка назви" Style="{StaticResource Label}"/><TextBox Text="{Binding Settings.Supplier.NameColumn, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True, NotifyOnValidationError=True}"/>
       <TextBlock Text="Перший рядок товарів" Style="{StaticResource Label}"/><TextBox Text="{Binding Settings.Supplier.FirstDataRow, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True, NotifyOnValidationError=True}"/>
       <TextBlock Text="Аркуш (порожньо — перший)" Style="{StaticResource Label}"/><TextBox Text="{Binding Settings.Supplier.SheetName, UpdateSourceTrigger=PropertyChanged}"/>
      </StackPanel>
      <StackPanel><TextBlock Text="Виключення · Excel" FontWeight="SemiBold" Style="{StaticResource Label}"/>
       <TextBlock Text="Колонка артикула" Style="{StaticResource Label}"/><TextBox Text="{Binding Settings.Exclusions.SkuColumn, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True, NotifyOnValidationError=True}"/>
       <TextBlock Text="Перший рядок товарів" Style="{StaticResource Label}"/><TextBox Text="{Binding Settings.Exclusions.FirstDataRow, UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True, NotifyOnValidationError=True}"/>
       <TextBlock Text="Аркуш (порожньо — перший)" Style="{StaticResource Label}"/><TextBox Text="{Binding Settings.Exclusions.SheetName, UpdateSourceTrigger=PropertyChanged}"/>
      </StackPanel>
     </UniformGrid>
     <TextBlock Text="Формат результату" Style="{StaticResource Label}"/><ComboBox Width="135" HorizontalAlignment="Left" ItemsSource="{x:Static local:MissingProductsViewModel.Formats}" DisplayMemberPath="Label" SelectedValuePath="Value" SelectedValue="{Binding Settings.OutputFormat}" IsEnabled="{Binding CanEdit}" ToolTip="Формат результату: артикул і назва"/>
    </StackPanel></ScrollViewer>
   </TabItem>
   <TabItem Header="3  Результат">
    <Grid Margin="0,12,0,0"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
     <UniformGrid x:Name="SummaryCards" Columns="4" Margin="0,0,0,10">
      <Button Tag="0" Click="Summary_Click" Style="{StaticResource SummaryButton}" Padding="8"><StackPanel><TextBlock Text="У постачальника" Style="{StaticResource Hint}"/><TextBlock Text="{Binding Summary.SupplierUnique, TargetNullValue=—, StringFormat=N0}" FontSize="23"/></StackPanel></Button>
      <Button Tag="1" Click="Summary_Click" Style="{StaticResource SummaryButton}" Padding="8"><StackPanel><TextBlock Text="Уже є на сайті" Style="{StaticResource Hint}"/><TextBlock Text="{Binding Summary.OnSite, TargetNullValue=—, StringFormat=N0}" FontSize="23"/></StackPanel></Button>
      <Button Tag="2" Click="Summary_Click" Style="{StaticResource SummaryButton}" Padding="8"><StackPanel><TextBlock Text="Виключено з пошуку" Style="{StaticResource Hint}"/><TextBlock Text="{Binding Summary.Excluded, TargetNullValue=—, StringFormat=N0}" FontSize="23"/></StackPanel></Button>
      <Button Tag="3" Click="Summary_Click" Style="{StaticResource SummaryButton}" Padding="8"><StackPanel><TextBlock Text="Можна додати на сайт" Style="{StaticResource Hint}"/><TextBlock Text="{Binding Summary.Products.Count, TargetNullValue=—, StringFormat=N0}" FontSize="23" Foreground="#126A55"/></StackPanel></Button>
     </UniformGrid>
     <DockPanel Grid.Row="1" Margin="0,0,0,8"><TextBox Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}" ToolTip="Пошук артикула або назви" AutomationProperties.Name="Пошук відсутніх товарів"/></DockPanel>
     <DataGrid x:Name="ResultGrid" Grid.Row="2" ItemsSource="{Binding Rows}" SelectionMode="Extended" SelectionUnit="FullRow" IsEnabled="{Binding CanEdit}">
      <DataGrid.Columns><DataGridTemplateColumn Header="Виключити" Width="100"><DataGridTemplateColumn.CellTemplate><DataTemplate><CheckBox IsChecked="{Binding IsExcluded, Mode=OneWay}" Click="Exclusion_Click" HorizontalAlignment="Center" Margin="0" AutomationProperties.Name="Виключити артикул"/></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn><DataGridTemplateColumn Header="Артикул" Width="180"><DataGridTemplateColumn.CellTemplate><DataTemplate><Button Content="{Binding Sku}" Click="CopySku_Click" ToolTip="Натисніть, щоб скопіювати артикул" Padding="4,0" Background="Transparent" BorderBrush="Transparent" Foreground="#126A55" HorizontalAlignment="Left"/></DataTemplate></DataGridTemplateColumn.CellTemplate></DataGridTemplateColumn><DataGridTextColumn Header="Назва" Binding="{Binding Name}" Width="*"/></DataGrid.Columns>
     </DataGrid>
     <StackPanel Grid.Row="3"><TextBlock Text="{Binding ResultCount}" Style="{StaticResource Hint}"/></StackPanel>
    </Grid>
   </TabItem>
   <TabItem Header="Виключення">
    <Grid Margin="0,12,0,0"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
     <TextBlock Text="{Binding ExclusionCount, StringFormat=Збережено {0:N0} артикулів. Цей список використовується при кожному пошуку.}" Style="{StaticResource Hint}" Margin="0,0,0,8"/>
     <DockPanel Grid.Row="1" Margin="0,0,0,8"><Button DockPanel.Dock="Right" Content="Зберегти виключення…" Click="ExportExclusions_Click" IsEnabled="{Binding CanExportExclusions}" Margin="8,0,0,0" ToolTip="Зберегти весь список. Формат CSV / XLSX — у налаштуваннях."/><Button DockPanel.Dock="Right" Content="Прибрати вибрані" Click="RemoveExclusions_Click" IsEnabled="{Binding CanEdit}" Margin="8,0,0,0"/><TextBox Text="{Binding ExclusionSearch, UpdateSourceTrigger=PropertyChanged}" ToolTip="Пошук у виключеннях"/></DockPanel>
     <ListBox x:Name="ExclusionList" Grid.Row="2" ItemsSource="{Binding Exclusions}" SelectionMode="Extended" IsEnabled="{Binding CanEdit}"><ListBox.ItemTemplate><DataTemplate><Button Content="{Binding}" Click="CopySku_Click" ToolTip="Натисніть, щоб скопіювати артикул" Padding="4,2" Background="Transparent" BorderBrush="Transparent" Foreground="#126A55" HorizontalAlignment="Left"/></DataTemplate></ListBox.ItemTemplate></ListBox>
    </Grid>
   </TabItem>
   <TabItem Header="Попередження">
    <Grid Margin="0,12,0,0"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
     <TextBlock Text="{Binding Warnings.Count, StringFormat=Попереджень: {0}}" Style="{StaticResource Hint}" Margin="0,0,0,8"/>
     <ListBox x:Name="WarningsList" Grid.Row="1" ItemsSource="{Binding Warnings}" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled" HorizontalContentAlignment="Stretch"><ListBox.ItemTemplate><DataTemplate><TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="5"/></DataTemplate></ListBox.ItemTemplate></ListBox>
    </Grid>
   </TabItem>
  </TabControl>
  <StackPanel x:Name="ModuleFooter" Grid.Row="1" Margin="0,8,0,0">
   <ProgressBar Height="3" IsIndeterminate="True" Visibility="{Binding IsBusy, Converter={StaticResource BoolVisibility}}"/>
   <TextBlock Text="{Binding Message}" TextWrapping="Wrap" MaxHeight="55" Margin="0,5,0,8"/>
   <StackPanel x:Name="ActionButtons" Orientation="Horizontal" HorizontalAlignment="Right"><Button Content="Скасувати" Click="Cancel_Click" Visibility="{Binding IsBusy, Converter={StaticResource BoolVisibility}}" Margin="8,0,0,0"/><Button Content="Знайти товари" Click="Analyze_Click" IsEnabled="{Binding CanAnalyze}" Margin="8,0,0,0"/><Button Content="Зберегти результат…" Click="Export_Click" IsEnabled="{Binding CanExport}" Style="{StaticResource PrimaryButton}" Margin="8,0,0,0"/></StackPanel>
  </StackPanel>
 </Grid>
</UserControl>
