# Image module plan

Approved: independent Ukrainian module; Makita gallery/direct image URLs/local files; default crop square 1000px, alternate padding; persistent global defaults, per-import overrides and per-image settings; crop position/zoom, before/after preview; local AI upscaling without quotas; JPG/PNG/WebP save to user-selected folder.

- [x] Infrastructure: bounded downloads, Makita gallery extraction, orientation-aware image processing and exact square output. Tests use generated non-square images and gallery HTML.
- [x] Local AI: pinned official Real-ESRGAN Windows archive, checksum, one-time installation, cancellation, explicit GPU failure; preview/export use same processor.
- [x] Desktop: isolated session and settings, source/preview/thumbs, commands and native file/folder dialogs. Never overwrite source or silently overwrite an export.
- [x] React: common tabs, load settings copied from defaults, per-image editor with draggable framing and before/after preview, progress/cancel, batch save and instructions.
- [x] Verify: typecheck, Core/Infrastructure and Desktop tests, native WebView scenario/screenshots, actual Makita download and local engine smoke test, Windows installer.
